#include "connectivity.h"

#include <string.h>

void init_buffer(void)
{
    memset(&buffer, 0, sizeof(buffer));
}

void read_instructions(void)
{
/*    int pos = 0;

    do {
        while(!U1STAbits.URXDA);
    } while(U1RXREG != 0x55);

    for(pos = 0; pos < sizeof(struct buffer); pos++) {
        while(!U1STAbits.URXDA);
        buffer[pos] = U1RXREG;
    }*/
}
